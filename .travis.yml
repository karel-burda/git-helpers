stages:
  - test

jobs:
  include:
     - stage: test
       dist: trusty
       os: linux
       addons:
         apt:
           packages:
             - shellcheck
       script:
         - set -e

         - ./tests/integration/run-all-tests.sh

         - set +e
